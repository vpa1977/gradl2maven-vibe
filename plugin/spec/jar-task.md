# Your task

Fix Jar task implementation  private void handleJarTask(Task task) {

# How it should work

Read Jar task properties and generate a string with `jar` command line call. 
Use several chained calls if necessary. 

