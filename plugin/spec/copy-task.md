# Your task

Implement handleCopyTask.

# How to implement

- use task.getSource().getFiles() to get files to be copied
- The destination is task.getDestinationDir() + relative path to the file from task.getSource() root.